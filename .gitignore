# environments / caches
.venv/
__pycache__/
*.pyc
.DS_Store
# app-generated copies of delivered files
Claude outputs/
# run artefacts: keep RUNLOG/REPORT/results.json/summary.png, drop weights, states, logs, big arrays
runs/**/*.pt
# weights anywhere: --out defaults to octave_flow_out/ and can point outside runs/
*.pt
octave_flow_out/
runs/**/*.npz
runs/**/*.npy
runs/*.log
runs/**/*.log
*.npy
*.npz
*.bigfile
# LaTeX build files
notes/*.aux
notes/*.log
notes/*.out
notes/*.toc
notes/*.fls
notes/*.fdb_latexmk
notes/*.synctex.gz
